query GetRecentRequests {
  users {
    employeeName
    requests {
      requestDescription
    }
  }
}

query GetLogHistory {
  users {
    employeeName
    requests {
      requestDescription
    }
  }
}
