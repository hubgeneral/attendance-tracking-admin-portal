query getUsers {
  users {
    staffId
    employeeName
    email
    status
    employeeType
  }
}

query getUserById($id: Int!) {
  userById(id: $id) {
    staffId
    userName
    email
    status
    phoneNumber
  }
}
